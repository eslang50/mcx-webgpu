layout(set = 0, binding = 0) uniform SourceData {
    vec3 sourceOrigin;
    vec3 sourceDirection;
    float energy;
};

void main() {
    Ray ray;
    ray.origin = sourceOrigin;
    ray.direction = sourceDirection;
    ray.tmin = 0.0;
    ray.tmax = FLT_MAX;
    emit ray;
}
